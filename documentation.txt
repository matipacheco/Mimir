Para instalar scrapy:
	conda install -c conda-forge scrapy
	(verificar si se tienen instaladas todas las dependencias)

Para instalar MySQLdb:
	conda install -c anaconda mysql-python=1.2.5

Para instalar peewee:
	git clone https://github.com/coleifer/peewee.git
	cd peewee
	python setup.py